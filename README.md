This project is currently on hold due to a lack of Readme-Driven Design (RDD), which led to major architectural mistakes.

However, you can still test it by running: 
```
# Install Rust toolchain if u havent (includes cargo)
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh

# Clone and run project
git clone git@github.com:n4zguu1/awqat.git
cd awqat
cargo run
```
